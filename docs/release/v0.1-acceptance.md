# LumaRoute v0.1 Acceptance

Evidence map for design §14. Checkboxes receive a CI run URL or tester/date/OS note in the release PR. Failed evidence blocks release.

## Automated evidence

- [x] Two logical servers, each with two ServerId-validated lines. — `tests/e2e/onboarding.spec.ts` (local Playwright)
- [x] Timeout and 503 browsing fail over; 401 does not. — `tests/integration/line-failover.test.ts`
- [x] Playback startup failure regenerates, loads, and reports with the backup-line plan.
  - Evidence: `tests/integration/playback-startup-failover.test.ts`; https://github.com/yangshulin-g/LumaRoute/actions/runs/32471750054
  - Limitations: pre-start only; playback interruption failover is out of scope
- [x] Movie, series, season, and episode browsing. — domain + adapter unit tests; E2E browses Movies library
- [x] Current-server search. — `tests/e2e/browse-search-play.spec.ts`
- [x] mpv direct play and remux-only direct stream; transcode-only rejection. — adapter/player unit tests; E2E uses `FakePlayerEngine` (not real mpv)
- [x] Progress normally remains within 10 seconds. — `browse-search-play` asserts seeked progress `PositionTicks`; unit cadence in `ProgressReporter`
- [x] Full quality gate.
  - Evidence: https://github.com/yangshulin-g/LumaRoute/actions/runs/32471750054 on `8ee313815304fa1b09ded809be65d1f4edbf4155`
  - Limitations: later quality matrix is Windows x64 and macOS Apple Silicon only
- [x] Real mpv decodes H.264/H.265/AV1 and completes JSON IPC on active package runners.
  - Evidence: installed qualification logs on https://github.com/yangshulin-g/LumaRoute/actions/runs/33354768101 ; Windows job `99374707883` completed 2026-08-31T04:08:02Z; Apple Silicon job `99374707893` completed 2026-08-31T03:49:15Z
  - Limitations: software decode is allowed; high-bitrate hardware performance is not claimed
- [x] Windows x64 MSI/NSIS and macOS Apple Silicon DMG have verified SHA-256 siblings.
  - Evidence: https://github.com/yangshulin-g/LumaRoute/actions/runs/33354768101 ; offline verify of `LumaRoute_0.1.0_x64_en-US.msi`, `LumaRoute_0.1.0_x64-setup.exe`, `LumaRoute_0.1.0_aarch64.dmg` (`PASS verified 3 package checksum(s)`)
  - Limitations: unsigned/unnotarized Internal Alpha only; Ubuntu job hung on mpv qualify and was cancelled after the two hard-gate jobs succeeded
- [ ] Linux x64 quality, package, and native mpv qualification.
  - Evidence: deferred after Ubuntu package job `99374707699` hung on `verify-mpv.mjs installed` from 2026-08-31T03:44:37Z; no Linux installers or native qualify logs were collected
  - Limitations: not an Internal Alpha hard gate; Windows/Apple Silicon evidence is not a Linux substitute; `x86_64-unknown-linux-gnu` remains `archive-sealed`
- [ ] macOS Intel quality, package, and native mpv qualification.
  - Evidence: deferred because a `macos-13` runner is unavailable
  - Limitations: Apple Silicon evidence is not an Intel substitute; `x86_64-apple-darwin` remains unproven
- [x] Credential leakage scans return zero findings. — `pnpm check:sensitive` / `check` gate; https://github.com/yangshulin-g/LumaRoute/actions/runs/32471750054

## Deferred Jellyfin evidence

- [ ] Temporary Jellyfin container authenticates, browses, searches, plans playback, and reports progress.
  - Evidence: deferred from the Emby-first Internal Alpha; no successful live-contract URL recorded
  - Limitations: adapter and fixed-fixture tests remain; live container and real-system validation are deferred and do not block this Alpha

## Internal Alpha real-system matrix

- [ ] macOS Apple Silicon × Emby
  - Evidence: not yet executed
  - Required: install; first launch or credential read; browse/detail; direct play or remux; pause/resume/seek/stop; started/progress/paused/seeked/stopped write-back; app-exit mpv/IPC cleanup; uninstall
  - Limitations: execution environment not yet supplied
- [ ] macOS × Jellyfin
  - Evidence: deferred from Emby-first Internal Alpha
  - Required: same closed loop as macOS Apple Silicon × Emby in a later compatibility stage
  - Limitations: not an Internal Alpha hard gate
- [ ] macOS Intel × Emby/Jellyfin
  - Evidence: deferred because a `macos-13` runner is unavailable
  - Required: native Intel CI and real-system evidence in a later compatibility stage
  - Limitations: Apple Silicon evidence is not an Intel substitute
- [ ] Windows x64 × Emby
  - Evidence: not yet executed
  - Required: same closed loop as macOS Apple Silicon × Emby
  - Limitations: execution environment not yet supplied
- [ ] Windows x64 × Jellyfin
  - Evidence: deferred from Emby-first Internal Alpha
  - Required: same closed loop as macOS Apple Silicon × Emby in a later compatibility stage
  - Limitations: not an Internal Alpha hard gate

## Real-system release checks

- [ ] Windows x64 clean install, launch, uninstall, and same-version repair.
- [ ] macOS Intel clean install, launch, and uninstall. — deferred; no Intel runner/device evidence, and Apple Silicon is not a substitute
- [ ] macOS Apple Silicon clean install, launch, and uninstall.
- [ ] Linux x64 AppImage launch and removal. — deferred; not an Internal Alpha hard gate
- [ ] Linux x64 deb install, launch, upgrade simulation, and uninstall. — deferred; not an Internal Alpha hard gate
- [ ] Every artifact SHA-256 matches its sibling file.
- [ ] Public macOS build is signed/notarized and public Windows build is signed.
- [ ] Project license is selected before any public release is described as open source.

## Spec §14 outcome mapping

| # | Outcome | Evidence |
|---|---------|----------|
| 1 | ≥2 logical servers, ≥2 lines each | E2E onboarding |
| 2 | Backup lines require ServerId match | line-failover + line-service unit tests |
| 3 | Timeout/503 browse failover | line-failover integration |
| 4 | Playback start failure → backup plan | playback-startup-failover integration; https://github.com/yangshulin-g/LumaRoute/actions/runs/32471750054 |
| 5 | Browse movie/series/season/episode | media adapter + UI tests |
| 6 | Search current server | E2E browse-search-play |
| 7 | Direct play / remux direct stream | adapter + player tests |
| 8 | Progress skew usually ≤10s | ProgressReporter + E2E progress assert |
| 9 | Win/Apple Silicon package artifacts | https://github.com/yangshulin-g/LumaRoute/actions/runs/33354768101 |
| 10 | No credentials in logs/errors | sensitive-output scanner |

## Leftovers (environment)

1. Run the deferred live Jellyfin contract in a later compatibility stage; do not treat it as an Internal Alpha blocker.
2. Complete Linux quality/package and native mpv qualification in a later compatibility stage; do not treat Ubuntu as an Internal Alpha blocker.
3. Complete real-system install/uninstall matrix on release hardware.
4. Sign/notarize public builds when release credentials exist.
